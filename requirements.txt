# What the dashboard needs to RUN — local or deployed.
# Training, scraping, clustering and evaluation live in requirements-dev.txt.
#
# Every version is pinned to what this project was built and measured with, so
# the numbers in README.md are reproducible. Python 3.11.

# Ask pip for the CPU build of torch. Without this line, installing torch on
# Linux drags in the CUDA runtime — about 2.5 GB of nvidia-* wheels for a GPU no
# deployment target here has. The +cpu local version sorts above the plain one,
# so pip prefers it; on macOS this index serves the same wheels as PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.62.0
pandas==2.2.3
numpy==2.4.6
plotly==7.0.0
psycopg2-binary==2.9.10
python-dotenv==1.0.1
tqdm==4.67.1

torch==2.13.0
transformers==5.16.1
sentence-transformers==6.0.1
huggingface-hub==1.29.0
faiss-cpu==1.15.0
