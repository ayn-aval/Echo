# Phase 1 — data collection
# Grows per phase; pinned properly in Phase 9.
google-play-scraper==1.2.7
psycopg2-binary==2.9.10
python-dotenv==1.0.1
pandas==2.2.3
tqdm==4.67.1
matplotlib==3.11.1

# Phase 2 — baselines and evaluation harness
torch==2.13.0
transformers==5.16.1
datasets==5.0.1
scipy==1.17.1
scikit-learn==1.9.0
streamlit==1.62.0

# Phase 4 — domain adaptation (sentence-transformers allowed from here on)
sentence-transformers==6.0.1
accelerate==1.14.0

# Phase 5 — theme discovery
umap-learn==0.5.12
hdbscan==0.8.44
plotly==7.0.0

# Phase 6 — semantic search
faiss-cpu==1.15.0
