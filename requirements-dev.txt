# Everything above what the app needs to run: data collection, model training,
# clustering, evaluation, and the screenshot harness.
#
#     pip install -r requirements-dev.txt
#
# This is the file to install when reproducing the project end to end. The plain
# requirements.txt is deliberately smaller, because it is also what the deployed
# server installs and umap/hdbscan/datasets have no part in serving a dashboard.

-r requirements.txt

# Phase 1 — data collection
google-play-scraper==1.2.7
matplotlib==3.11.1

# Phase 2/3 — training and the STS benchmarks
datasets==5.0.1
scipy==1.17.1
scikit-learn==1.9.0
accelerate==1.14.0

# Phase 5 — theme discovery
umap-learn==0.5.12
hdbscan==0.8.44

# Verification only: eval/shoot_app.py drives real Chrome to photograph every
# dashboard screen, because no automated test can tell that a screen looks wrong.
playwright==1.62.0
